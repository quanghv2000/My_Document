/*
    Map.size()
    - Returns the number of items in a Map
    - Return Number
*/

const map = new Map();

map.set("a", "alpha");
map.set("b", "beta");
map.set("g", "gamma");

console.log(map.size);
// expected output: 3
